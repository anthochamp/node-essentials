{
	"name": "@ac-essentials/app-util",
	"version": "0.1.0",
	"type": "module",
	"main": "./src/index.ts",
	"publishConfig": {
		"access": "public",
		"main": "./dist/index.js"
	},
	"files": [
		"dist"
	],
	"dependencies": {
		"@ac-essentials/cli": "workspace:*",
		"@ac-essentials/misc-util": "workspace:*",
		"cosmiconfig": "^9.0.0",
		"find-up": "^7.0.0",
		"type-fest": "^4.41.0"
	},
	"devDependencies": {
		"@ac-essentials/tsconfig": "workspace:*",
		"@ac-essentials/typedoc": "workspace:*",
		"@types/node": "22.18.0",
		"rimraf": "6.0.1",
		"typedoc": "0.28.12",
		"typescript": "5.9.2",
		"vitest-mock-extended": "3.1.0"
	},
	"scripts": {
		"compile": "tsc -p ./tsconfig.build.json -outDir dist",
		"tsc:check": "tsc -noEmit -p ./tsconfig.json",
		"clean": "rimraf dist",
		"build": "yarn clean && yarn compile",
		"test": "yarn run -T vitest run  --project @ac-essentials/app-util",
		"prepack": "yarn build",
		"prepublish": "yarn test"
	}
}
