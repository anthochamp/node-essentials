/** EUI-48 — the MAC address of IEEE 802 — is 48 bits wide. */
export const EUI48_BIT_WIDTH = 48;

/** EUI-64 is 64 bits wide. */
export const EUI64_BIT_WIDTH = 64;

/** An OUI is the leading 24 bits of an EUI. */
export const OUI_BIT_LENGTH = 24;
