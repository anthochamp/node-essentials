{
	"name": "@ac-essentials/cli",
	"version": "0.1.4",
	"type": "module",
	"main": "./src/index.ts",
	"publishConfig": {
		"access": "public",
		"main": "./dist/index.js"
	},
	"files": [
		"dist"
	],
	"sideEffects": false,
	"dependencies": {
		"@ac-essentials/misc-util": "workspace:^",
		"type-fest": "^5.0.1"
	},
	"devDependencies": {
		"@ac-essentials/tsconfig": "workspace:*",
		"@ac-essentials/typedoc-config": "workspace:*",
		"@types/node": "24.12.0",
		"rimraf": "6.1.3",
		"typedoc": "0.28.18",
		"typescript": "6.0.2"
	},
	"scripts": {
		"compile": "tsc -p ./tsconfig.build.json -outDir dist",
		"tsc:check": "tsc -noEmit -p ./tsconfig.json",
		"clean": "rimraf dist",
		"build": "yarn clean && yarn compile",
		"test": "yarn run -T vitest run  --project $npm_package_name",
		"prepack": "yarn build"
	}
}
