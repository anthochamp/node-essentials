/**
 * Result of a three-way comparison: `-1` when the left operand is strictly
 * smaller, `0` when both operands are equal, `1` when the left operand is
 * strictly greater.
 */
export type Sign = -1 | 0 | 1;
