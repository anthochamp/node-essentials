export type MarkdownTableOptions = {
	/** Emit alignment colons in the delimiter row. Default `true`. */
	readonly alignment?: boolean;
	/** Locale for number and date formatting. */
	readonly locale?: string | string[];
};
