# app-logger-example

Runnable example application for `@ac-kit/app-logger`. Private — not published,
and excluded from the package tarball by `@ac-kit/app-logger`'s `files` field.

Run it from the repository root:

```sh
yarn workspace app-logger-example start
```
