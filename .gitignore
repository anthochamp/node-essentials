.temp/
dist/
node_modules/
*-generated.*
generated/
*.generated.*

# Astro type-generation and content cache
.astro/

# TypeDoc markdown, regenerated into the content collection on every site build
website/*/src/content/docs/api/

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Rust / WASM build artefacts
target/
*.wasm
